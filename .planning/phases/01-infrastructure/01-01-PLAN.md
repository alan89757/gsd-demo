---
phase: 01-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: []
---

<objective>
初始化 Vue 3 + TypeScript + Vite 项目，创建标准目录结构，安装核心依赖，配置开发工具。

Purpose: 为整个项目奠定基础，建立开发规范和代码组织结构
Output: 完整的项目脚手架，依赖安装完成，目录结构就绪
</objective>

<execution_context>
@$HOME/.trae/get-shit-done/workflows/execute-plan.md
@$HOME/.trae/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/research/STACK.md
@.planning/phases/01-infrastructure/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>初始化 Vue 3 + Vite 项目</name>
  <files>
    package.json
    vite.config.ts
    tsconfig.json
    index.html
    .eslintrc.cjs
    .prettierrc
  </files>
  <action>
使用 Vite 创建 Vue 3 + TypeScript 项目：

```bash
npm create vite@latest admin-system -- --template vue-ts
cd admin-system
npm install
```

项目创建后，清理默认文件，保留基本结构。

配置 vite.config.ts:
- 设置 @ 别名指向 src
- 配置 proxy 用于开发环境 API 代理
- 启用 path aliases

配置 tsconfig.json:
- 设置 strict 模式
- 配置 @ 别名路径映射
- 设置 baseUrl 为 ./src

安装核心依赖:
```bash
npm install vue-router@4 pinia axios element-plus @element-plus/icons-vue
npm install -D sass
```
  </action>
  <verify>
    <automated>npm run build 成功，无错误</automated>
  </verify>
  <done>Vue 3 + Vite + TypeScript 项目创建成功，package.json 包含 vue, vue-router, pinia, axios, element-plus</done>
</task>

<task type="auto">
  <name>创建标准目录结构</name>
  <files>
    src/api/index.ts
    src/api/modules/.gitkeep
    src/assets/styles/variables.scss
    src/assets/styles/global.scss
    src/components/common/.gitkeep
    src/components/layout/.gitkeep
    src/composables/.gitkeep
    src/layouts/DefaultLayout.vue
    src/layouts/BlankLayout.vue
    src/router/index.ts
    src/router/routes.ts
    src/router/guards.ts
    src/stores/index.ts
    src/stores/user.ts
    src/stores/permission.ts
    src/types/api.d.ts
    src/types/user.d.ts
    src/utils/request.ts
    src/utils/storage.ts
    src/utils/validate.ts
    src/utils/format.ts
    src/views/login/index.vue
    src/views/dashboard/index.vue
  </files>
  <action>
根据架构研究创建标准目录结构：

```
src/
├── api/                    # API接口定义
│   └── modules/           # 按模块划分
├── assets/                # 静态资源
│   ├── images/
│   └── styles/           # 全局样式
│       ├── variables.scss # SCSS变量
│       └── global.scss   # 全局样式
├── components/            # 公共组件
│   ├── common/           # 通用组件
│   └── layout/           # 布局组件
├── composables/          # 组合式函数
├── layouts/              # 布局模板
├── router/               # 路由配置
├── stores/               # Pinia状态库
├── types/                # TypeScript类型定义
├── utils/                # 工具函数
└── views/                # 页面组件
```

创建目录后，创建占位文件（空文件或简单导出）。

在 src/assets/styles/variables.scss 中定义主题变量：
- 主题色
- 侧边栏宽度
- 头部高度
- 常用间距

在 src/assets/styles/global.scss 中添加全局样式：
- 引入 Element Plus 样式
- 引入自定义变量
- 基础重置样式
  </action>
  <verify>
    <automated>ls -la src/ 显示完整目录结构</automated>
  </verify>
  <done>目录结构创建完成，所有目录和占位文件就绪</done>
</task>

<task type="auto">
  <name>配置 Element Plus 和应用入口</name>
  <files>
    src/main.ts
    src/App.vue
    src/env.d.ts
  </files>
  <action>
配置 Element Plus 按需引入：

创建 src/main.ts:
```typescript
import { createApp } from 'vue'
import { createPinia } from 'pinia'
import ElementPlus from 'element-plus'
import 'element-plus/dist/index.css'
import * as ElementPlusIcons from '@element-plus/icons-vue'

import App from './App.vue'
import router from './router'
import '@/assets/styles/global.scss'

const app = createApp(App)

// 注册所有 Element Plus 图标
for (const [key, component] of Object.entries(ElementPlusIcons)) {
  app.component(key, component)
}

app.use(createPinia())
app.use(router)
app.use(ElementPlus)

app.mount('#app')
```

更新 src/App.vue 为简单的路由容器:
```vue
<template>
  <router-view />
</template>
```

创建 src/env.d.ts 配置 Vite 环境变量类型:
```typescript
/// <reference types="vite/client" />

interface ImportMetaEnv {
  readonly VITE_API_BASE_URL: string
}

interface ImportMeta {
  readonly env: ImportMetaEnv
}
```

创建 .env.development:
```
VITE_API_BASE_URL=/api
```

创建 .env.production:
```
VITE_API_BASE_URL=https://api.example.com
```
  </action>
  <verify>
    <automated>npm run dev 成功启动，无编译错误</automated>
  </verify>
  <done>Element Plus 配置完成，应用入口文件就绪，环境变量配置完成</done>
</task>

</tasks>

<verification>
- 项目可成功运行 `npm run dev`
- Element Plus 组件可正常使用
- TypeScript 无类型错误
</verification>

<success_criteria>
1. 项目初始化完成，依赖安装成功
2. 标准目录结构创建完成
3. Element Plus 按需引入配置完成
4. 环境变量配置完成
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure/01-01-SUMMARY.md`
</output>

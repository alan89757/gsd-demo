---
phase: 01-infrastructure
plan: 04
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - src/utils/request.ts
  - src/api/index.ts
  - src/api/modules/auth.ts
  - src/types/api.d.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-04
---

<objective>
封装 Axios 请求库，实现请求/响应拦截器，统一错误处理，创建基础 API 模块。

Purpose: 提供统一的 API 调用方式，统一处理 Token、错误和响应格式
Output: request 工具、auth API 模块
</objective>

<execution_context>
@$HOME/.trae/get-shit-done/workflows/execute-plan.md
@$HOME/.trae/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/01-infrastructure/01-CONTEXT.md
@.planning/phases/01-infrastructure/01-01-PLAN.md
@.planning/phases/01-infrastructure/01-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>封装 Axios 请求工具</name>
  <files>
    src/utils/request.ts
    src/types/api.d.ts
  </files>
  <action>
创建 src/types/api.d.ts 定义 API 响应类型:

```typescript
export interface ApiResponse<T = any> {
  code: number
  data: T
  message: string
}

export interface ApiError {
  code: number
  message: string
  details?: any
}

export interface PageParams {
  page: number
  pageSize: number
  total?: number
}

export interface PageResponse<T> {
  list: T[]
  total: number
  page: number
  pageSize: number
}
```

创建 src/utils/request.ts 封装 Axios（per D-09, D-10）:

```typescript
import axios, { AxiosInstance, AxiosRequestConfig, AxiosResponse, AxiosError } from 'axios'
import { ElMessage } from 'element-plus'
import { getToken } from './storage'
import router from '@/router'

const BASE_URL = import.meta.env.VITE_API_BASE_URL || '/api'

const service: AxiosInstance = axios.create({
  baseURL: BASE_URL,
  timeout: 30000,
  headers: {
    'Content-Type': 'application/json'
  }
})

// 请求拦截器 - 添加 Token
service.interceptors.request.use(
  (config) => {
    const token = getToken()
    if (token) {
      config.headers.Authorization = `Bearer ${token}`
    }
    return config
  },
  (error) => {
    console.error('Request error:', error)
    return Promise.reject(error)
  }
)

// 响应拦截器 - 统一处理
service.interceptors.response.use(
  (response: AxiosResponse<ApiResponse>) => {
    const res = response.data
    
    // 根据业务状态码处理
    if (res.code !== 200) {
      ElMessage.error(res.message || '请求失败')
      return Promise.reject(new Error(res.message || '请求失败'))
    }
    
    return res
  },
  async (error: AxiosError<ApiError>) => {
    // 处理 HTTP 错误
    const status = error.response?.status
    const errorData = error.response?.data

    switch (status) {
      case 400:
        ElMessage.error(errorData?.message || '请求参数错误')
        break
      case 401:
        // Token 过期或无效 - 清除登录状态并跳转登录页
        ElMessage.error('登录已过期，请重新登录')
        localStorage.clear()
        router.push('/login')
        break
      case 403:
        ElMessage.error('没有访问权限')
        break
      case 404:
        ElMessage.error('请求的资源不存在')
        break
      case 500:
        ElMessage.error('服务器错误，请稍后重试')
        break
      default:
        ElMessage.error(errorData?.message || '网络错误，请检查网络连接')
    }

    return Promise.reject(error)
  }
)

// 封装常用请求方法
export const request = {
  get: <T = any>(url: string, params?: any, config?: AxiosRequestConfig) => {
    return service.get<ApiResponse<T>>(url, { params, ...config })
  },
  
  post: <T = any>(url: string, data?: any, config?: AxiosRequestConfig) => {
    return service.post<ApiResponse<T>>(url, data, config)
  },
  
  put: <T = any>(url: string, data?: any, config?: AxiosRequestConfig) => {
    return service.put<ApiResponse<T>>(url, data, config)
  },
  
  delete: <T = any>(url: string, params?: any, config?: AxiosRequestConfig) => {
    return service.delete<ApiResponse<T>>(url, { params, ...config })
  }
}

export default service
```

关键实现：
1. 统一添加 Authorization header（Bearer Token）
2. 401 错误自动清除登录状态并跳转登录页
3. 友好的错误提示（AUTH-04）
4. 支持多种 HTTP 方法
5. 从 .env 文件读取 API 基础地址（per D-09）
  </action>
  <verify>
    <automated>TypeScript 编译无错误</automated>
  </verify>
  <done>Axios 封装完成，统一错误处理实现（AUTH-04）</done>
</task>

<task type="auto">
  <name>创建认证 API 模块</name>
  <files>
    src/api/modules/auth.ts
    src/api/index.ts
  </files>
  <action>
创建 src/api/modules/auth.ts 定义认证相关 API:

```typescript
import { request } from '@/utils/request'
import type { User, LoginForm } from '@/types/user'
import type { ApiResponse } from '@/types/api'

export interface LoginRequest {
  username: string
  password: string
}

export interface LoginResponse {
  token: string
  user: User
}

export interface UserInfoResponse {
  user: User
  permissions: string[]
  routes: string[]
}

export const authApi = {
  login: (data: LoginRequest) => {
    return request.post<LoginResponse>('/auth/login', data)
  },
  
  logout: () => {
    return request.post('/auth/logout')
  },
  
  getUserInfo: () => {
    return request.get<UserInfoResponse>('/auth/userinfo')
  },
  
  updatePassword: (oldPassword: string, newPassword: string) => {
    return request.post('/auth/password', {
      oldPassword,
      newPassword
    })
  }
}
```

创建 src/api/index.ts 统一导出 API 模块:

```typescript
export * from './modules/auth'
```

更新 userStore 的 login 方法，使用真实的 API 调用:

```typescript
async function login(form: LoginForm) {
  try {
    const response = await authApi.login(form)
    setUserToken(response.data.token)
    setUserInfoAction(response.data.user)
    
    // 获取用户权限信息
    const userInfoResponse = await authApi.getUserInfo()
    const permissionStore = usePermissionStore()
    permissionStore.setPermissionsAction(userInfoResponse.data.permissions)
    
    return response.data
  } catch (error) {
    // 登录失败，抛出错误由组件处理（AUTH-04）
    throw error
  }
}
```

对于 Phase 1 的演示模式，可以保留模拟数据，但保留 API 调用结构以便后续替换。
  </action>
  <verify>
    <automated>TypeScript 编译无错误</automated>
  </verify>
  <done>认证 API 模块创建完成，登录功能完整实现（AUTH-01）</done>
</task>

<task type="auto">
  <name>集成登录功能</name>
  <files>
    src/views/login/index.vue
    src/router/guards.ts
  </files>
  <action>
更新 src/views/login/index.vue 连接真实登录逻辑:

```typescript
import { defineComponent, ref } from 'vue'
import { useRouter } from 'vue-router'
import { useUserStore } from '@/stores'
import { ElMessage } from 'element-plus'

const LoginPage = defineComponent({
  setup() {
    const router = useRouter()
    const userStore = useUserStore()
    
    const loginForm = ref({
      username: '',
      password: ''
    })
    
    const loading = ref(false)
    
    const handleLogin = async () => {
      if (!loginForm.value.username || !loginForm.value.password) {
        ElMessage.warning('请输入用户名和密码')
        return
      }
      
      loading.value = true
      try {
        await userStore.login(loginForm.value)
        ElMessage.success('登录成功')
        router.push('/dashboard')
      } catch (error) {
        // 错误已在 request 拦截器中处理，这里只需要记录日志
        console.error('Login failed:', error)
      } finally {
        loading.value = false
      }
    }
    
    return {
      loginForm,
      loading,
      handleLogin
    }
  }
})

export default LoginPage
```

更新 src/router/guards.ts 实现完整的路由守卫:

```typescript
import { createRouter, createWebHistory, RouteRecordRaw } from 'vue-router'
import { getToken } from '@/utils/storage'

const routes: RouteRecordRaw[] = [
  {
    path: '/login',
    name: 'Login',
    component: () => import('@/views/login/index.vue'),
    meta: { requiresAuth: false }
  },
  {
    path: '/',
    component: () => import('@/layouts/DefaultLayout.vue'),
    redirect: '/dashboard',
    meta: { requiresAuth: true },
    children: [
      {
        path: 'dashboard',
        name: 'Dashboard',
        component: () => import('@/views/dashboard/index.vue'),
        meta: { title: '仪表盘', requiresAuth: true }
      }
    ]
  },
  {
    path: '/:pathMatch(.*)*',
    redirect: '/login'
  }
]

const router = createRouter({
  history: createWebHistory(),
  routes
})

// 路由守卫
router.beforeEach((to, from, next) => {
  const token = getToken()
  const requiresAuth = to.matched.some(record => record.meta.requiresAuth)
  
  if (requiresAuth && !token) {
    // 未登录，跳转到登录页（AUTH-05）
    next('/login')
  } else if (to.path === '/login' && token) {
    // 已登录，访问登录页跳转到首页
    next('/dashboard')
  } else {
    next()
  }
})

// 更新页面标题
router.afterEach((to) => {
  const title = to.meta.title as string
  document.title = title ? `${title} - 后台管理系统` : '后台管理系统'
})

export default router
```
  </action>
  <verify>
    <automated>npm run dev 成功启动，访问 /login 正常显示登录页</automated>
  </verify>
  <done>登录功能集成完成，路由守卫完善（AUTH-01, AUTH-05）</done>
</task>

</tasks>

<must_haves>
  truths:
    - "用户可以使用用户名和密码登录"
    - "登录失败时显示友好的错误提示"
    - "未登录用户访问受保护路由自动跳转登录页"
    - "请求自动携带 Token"
    - "API 错误显示友好提示"
  artifacts:
    - path: src/utils/request.ts
      provides: Axios 封装，统一请求处理
      exports: [request, service]
    - path: src/api/modules/auth.ts
      provides: 认证相关 API
      exports: [authApi.login, authApi.logout, authApi.getUserInfo]
    - path: src/views/login/index.vue
      provides: 登录页面
      contains: 表单, 验证, 提交处理
  key_links:
    - from: request.ts
      to: storage.ts
      via: getToken
      pattern: 请求拦截器获取 Token
    - from: request.ts
      to: router
      via: 401 错误处理
      pattern: Token 过期跳转登录
    - from: login/index.vue
      to: userStore
      via: login action
      pattern: 登录表单提交
</must_haves>

<verification>
- 使用用户名和密码可以登录
- 登录失败显示友好错误消息
- 未登录访问 /dashboard 自动跳转到 /login
- 登录成功后跳转到 /dashboard
</verification>

<success_criteria>
1. Axios 请求封装完成（AUTH-04 基础）
2. 认证 API 模块创建完成（AUTH-01 基础）
3. 登录页完整功能可用
4. Token 自动添加到请求头
5. 401 错误自动处理并跳转登录页
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure/01-04-SUMMARY.md`
</output>

---
phase: 01-infrastructure
plan: 03
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - src/stores/user.ts
  - src/stores/permission.ts
  - src/stores/index.ts
  - src/utils/storage.ts
autonomous: true
requirements:
  - AUTH-02
  - AUTH-03
  - PERM-01
  - PERM-04
---

<objective>
配置 Pinia 状态管理，创建用户状态和权限状态，实现 Token 存储和登录/登出逻辑。

Purpose: 管理用户登录状态、权限信息和会话持久化
Output: userStore、permissionStore、Token 存储工具
</objective>

<execution_context>
@$HOME/.trae/get-shit-done/workflows/execute-plan.md
@$HOME/.trae/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/01-infrastructure/01-CONTEXT.md
@.planning/phases/01-infrastructure/01-01-PLAN.md
@.planning/phases/01-infrastructure/01-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>创建 Token 存储工具</name>
  <files>
    src/utils/storage.ts
  </files>
  <action>
创建 src/utils/storage.ts 实现 Token 存储（per D-01）:

定义存储键常量:
```typescript
const TOKEN_KEY = 'admin_token'
const USER_INFO_KEY = 'admin_user_info'
const PERMISSIONS_KEY = 'admin_permissions'
```

导出 Token 存储函数:
```typescript
// 获取 Token
export function getToken(): string | null {
  return localStorage.getItem(TOKEN_KEY)
}

// 设置 Token
export function setToken(token: string): void {
  localStorage.setItem(TOKEN_KEY, token)
}

// 移除 Token
export function removeToken(): void {
  localStorage.removeItem(TOKEN_KEY)
}

// 获取用户信息
export function getUserInfo(): User | null {
  const info = localStorage.getItem(USER_INFO_KEY)
  return info ? JSON.parse(info) : null
}

// 设置用户信息
export function setUserInfo(user: User): void {
  localStorage.setItem(USER_INFO_KEY, JSON.stringify(user))
}

// 移除用户信息
export function removeUserInfo(): void {
  localStorage.removeItem(USER_INFO_KEY)
}

// 获取权限列表
export function getPermissions(): string[] {
  const perms = localStorage.getItem(PERMISSIONS_KEY)
  return perms ? JSON.parse(perms) : []
}

// 设置权限列表
export function setPermissions(permissions: string[]): void {
  localStorage.setItem(PERMISSIONS_KEY, JSON.stringify(permissions))
}

// 清除所有登录信息
export function clearAuth(): void {
  removeToken()
  removeUserInfo()
  localStorage.removeItem(PERMISSIONS_KEY)
}
```

定义用户信息接口（引用 src/types/user.d.ts 中的定义）
  </action>
  <verify>
    <automated>TypeScript 编译无错误</automated>
  </verify>
  <done>Token 存储工具创建完成（per D-01, D-02 基础）</done>
</task>

<task type="auto">
  <name>创建 userStore</name>
  <files>
    src/stores/user.ts
    src/types/user.d.ts
  </files>
  <action>
创建 src/types/user.d.ts 定义用户相关类型:

```typescript
export interface User {
  id: string
  username: string
  email: string
  avatar?: string
  role: string
  roleName: string
  createdAt: string
}

export interface LoginForm {
  username: string
  password: string
}

export interface LoginResponse {
  token: string
  user: User
}
```

创建 src/stores/user.ts 实现用户状态管理:

```typescript
import { defineStore } from 'pinia'
import { ref, computed } from 'vue'
import { getToken, setToken, removeToken, getUserInfo, setUserInfo, clearAuth } from '@/utils/storage'
import type { User, LoginForm, LoginResponse } from '@/types/user'

export const useUserStore = defineStore('user', () => {
  // State
  const token = ref<string>(getToken() || '')
  const userInfo = ref<User | null>(getUserInfo())

  // Getters
  const isLoggedIn = computed(() => !!token.value)
  const username = computed(() => userInfo.value?.username || '')
  const userId = computed(() => userInfo.value?.id || '')

  // Actions
  function setUserToken(newToken: string) {
    token.value = newToken
    setToken(newToken)
  }

  function setUserInfoAction(info: User) {
    userInfo.value = info
    setUserInfo(info)
  }

  async function login(form: LoginForm) {
    // TODO: 调用登录 API（Plan 04 实现）
    // 暂时使用模拟数据
    const mockResponse: LoginResponse = {
      token: 'mock_token_' + Date.now(),
      user: {
        id: '1',
        username: form.username,
        email: form.username + '@example.com',
        role: 'admin',
        roleName: '管理员',
        createdAt: new Date().toISOString()
      }
    }
    setUserToken(mockResponse.token)
    setUserInfoAction(mockResponse.user)
    return mockResponse
  }

  function logout() {
    token.value = ''
    userInfo.value = null
    clearAuth()
  }

  function resetToken() {
    logout()
  }

  return {
    // State
    token,
    userInfo,
    // Getters
    isLoggedIn,
    username,
    userId,
    // Actions
    setUserToken,
    setUserInfoAction,
    login,
    logout,
    resetToken
  }
})
```

实现登出逻辑（AUTH-03）：
- 清除 token 和用户信息
- 跳转到登录页
- 清除 localStorage 中的所有认证信息
  </action>
  <verify>
    <automated>TypeScript 编译无错误</automated>
  </verify>
  <done>userStore 创建完成，登录状态持久化实现（AUTH-02, AUTH-03）</done>
</task>

<task type="auto">
  <name>创建 permissionStore</name>
  <files>
    src/stores/permission.ts
    src/stores/index.ts
  </files>
  <action>
创建 src/stores/permission.ts 实现权限状态管理（per D-03, D-04）:

```typescript
import { defineStore } from 'pinia'
import { ref } from 'vue'
import { getPermissions, setPermissions } from '@/utils/storage'

export const usePermissionStore = defineStore('permission', () => {
  // State
  const permissions = ref<string[]>(getPermissions())
  const routes = ref<RouteConfig[]>([])

  // Actions
  function setPermissionsAction(perms: string[]) {
    permissions.value = perms
    setPermissions(perms)
  }

  function setRoutes(newRoutes: RouteConfig[]) {
    routes.value = newRoutes
  }

  // 检查用户是否有指定权限
  function hasPermission(permission: string | string[]): boolean {
    if (Array.isArray(permission)) {
      return permission.some(p => permissions.value.includes(p))
    }
    return permissions.value.includes(permission)
  }

  // 清除权限信息（用户退出或权限变更时调用）（PERM-04）
  function clearPermissions() {
    permissions.value = []
    routes.value = []
    setPermissions([])
  }

  return {
    // State
    permissions,
    routes,
    // Actions
    setPermissionsAction,
    setRoutes,
    hasPermission,
    clearPermissions
  }
})
```

更新 src/stores/index.ts 统一导出所有 store:

```typescript
import { createPinia } from 'pinia'

export const pinia = createPinia()

export { useUserStore } from './user'
export { usePermissionStore } from './permission'
```

在 userStore 的 logout 方法中调用 permissionStore 的 clearPermissions，实现 PERM-04：

```typescript
function logout() {
  // 清除用户信息
  token.value = ''
  userInfo.value = null
  clearAuth()
  
  // 清除权限信息
  const permissionStore = usePermissionStore()
  permissionStore.clearPermissions()
}
```

实现 PERM-01（根据用户角色显示对应菜单）：
- permissionStore 提供 routes 和 permissions 状态
- DefaultLayout.vue 从 store 获取菜单配置
- 根据用户权限动态过滤可显示的菜单项
  </action>
  <verify>
    <automated>TypeScript 编译无错误</automated>
  </verify>
  <done>permissionStore 创建完成，权限状态管理就绪（PERM-01, PERM-04）</done>
</task>

</tasks>

<must_haves>
  truths:
    - "用户登录后 token 存储在 localStorage"
    - "用户刷新页面后登录状态保持"
    - "用户退出登录后 token 和用户信息被清除"
    - "用户权限变更后需要重新登录"
    - "侧边栏菜单根据用户权限动态显示"
  artifacts:
    - path: src/utils/storage.ts
      provides: Token 存储和读取
      exports: [getToken, setToken, removeToken, getUserInfo, setUserInfo]
    - path: src/stores/user.ts
      provides: 用户状态管理
      exports: [useUserStore, token, userInfo, login, logout]
    - path: src/stores/permission.ts
      provides: 权限状态管理
      exports: [usePermissionStore, permissions, hasPermission]
  key_links:
    - from: userStore
      to: storage.ts
      via: getToken, setToken
      pattern: store 调用 storage
    - from: userStore.logout
      to: permissionStore.clearPermissions
      via: 跨 store 调用
      pattern: logout 清除权限
    - from: DefaultLayout.vue
      to: permissionStore
      via: usePermissionStore
      pattern: 菜单权限过滤
</must_haves>

<verification>
- 登录后 token 存储在 localStorage
- 刷新页面后 token 和用户信息从 localStorage 恢复
- 退出登录后 localStorage 中的认证信息被清除
- 权限变更时调用 clearPermissions 清除权限
</verification>

<success_criteria>
1. Token 存储在 localStorage（per D-01）
2. 用户登录后状态持久化，刷新页面不丢失（AUTH-02）
3. 用户可随时退出登录（AUTH-03）
4. 权限信息存储在 localStorage
5. 权限变更后用户需要重新登录才能生效（PERM-04）
6. 菜单项根据用户权限动态显示（PERM-01）
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure/01-03-SUMMARY.md`
</output>
